#include <stdio.h>
#include <stdlib.h>

int Disassemble5A22Op(unsigned char* code, int pc)
{
    unsigned char* instruction = code[pc];

    int opbytes = 1;

    switch(*instruction)
    {
        //TBC
    }

    return 0;
}
